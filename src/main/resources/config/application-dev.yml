spring:
  datasource:
    url: ${DEV_DB_URL}
    username: ${DEV_DB_USERNAME}
    password: ${DEV_DB_PASSWORD}
    driver-class-name: ${DEV_DB_DRIVER_NAME}

  jpa:
    properties.hibernate.default_schema: ${DEV_DB_DEFAULT_SCHEMA}
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: validate # DB - Spring Entity 유효성 체크

google:
  client:
    id: ${GOOGLE_CLIENT_ID}
    secret: ${GOOGLE_CLIENT_SECRET}

jwt:
  secret: ${JWT_SECRET}
  access.expiration: ${JWT_ACCESS_EXPIRATION}
  refresh.expiration: ${JWT_REFRESH_EXPIRATION}
