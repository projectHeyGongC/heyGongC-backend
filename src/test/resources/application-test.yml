spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

jwt:
  secret: testtesttesttesttesttesttesttesttesttest
  access.expiration: 600000 # 10분
  refresh.expiration: 1800000 # 30분
  long.access.expiration: 3600000 # 60분

oauth:
  apple:
    iss: https://appleid.apple.com
    client-id: test
    nonce: nonce
